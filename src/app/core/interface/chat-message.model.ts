export interface ChatMessage{
    message :string;
    user:string;    
    messageDate?: Date; // Add optional property if needed
  authorityMail?: string; // Add optional property if needed
  busId?: string;
}